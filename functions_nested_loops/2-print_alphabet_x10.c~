#include "main.h"

/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */

void print_alphabet_x10(void)
{

  char c;
  for (c = 'a'; c <= 'z'; c++)

    {
      _putchar (c*10);
    }
  _putchar ('\n');
    
  return (0);
  
}
