#include <stdio.h>

/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */

int main(void)
{
  char i;

  for (i = 30; i < 40; i++)
    {
      putchar(i);
    }
  putchar('\n');
  return (0);
}
